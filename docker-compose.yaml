version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:3000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
